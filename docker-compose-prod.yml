version: "3"
  
services:
  app:
    build: .
    command: bash -c 'python manage.py migrate --noinput && python manage.py runserver 0.0.0.0:8000 -v2'
    volumes:
      - .:/app
      - ./static:/app/static
      - ./media:/app/media
    ports:
      - "8000:8000"
    env_file:
      - .env